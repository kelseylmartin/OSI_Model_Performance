% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{analyze_reviewer_effort}
\alias{analyze_reviewer_effort}
\title{Analyze Reviewer Effort by Comparing Raw and Validated Detections}
\usage{
analyze_reviewer_effort(raw_df, validated_df, group_vars = NULL)
}
\arguments{
\item{raw_df}{A data frame of raw model detections (e.g., from a VIAME CSV).
Must contain \code{TrackID} and \code{Species}.}

\item{validated_df}{A data frame of the same detections after human review.
Must contain \code{TrackID} and \code{Species}.}

\item{group_vars}{A character vector of column names to group the summary by
(e.g., \code{c("Deployment", "UniqFrame")}).}
}
\value{
A \code{tibble} summarizing the reviewer effort metrics for each group.
}
\description{
Compares raw model detections to a human-validated set to quantify the
effort required for correction. This is useful for understanding model
behavior, such as over-segmentation or misclassification.
}
\details{
The function calculates:
\itemize{
\item \code{n_raw}: The total number of raw model detections.
\item \code{n_validated}: The number of detections remaining after validation.
\item \code{n_deleted}: The number of raw detections deleted by the reviewer.
\item \code{n_reclassified}: The number of detections where the species class was changed.
\item \code{avg_raw_per_validated}: The average number of raw detections that correspond
to a single final validated detection, indicating over-segmentation.
}
}
\examples{
raw_detections <- dplyr::tibble(
  Deployment = "D1", UniqFrame = 1, TrackID = 1:5,
  Species = c("seal", "seal", "rock", "seal", "glare")
)
# Reviewer merges tracks 1,2,4 into a single "seal" (track 2),
# deletes track 3 ("rock") and 5 ("glare").
validated_detections <- dplyr::tibble(
  Deployment = "D1", UniqFrame = 1, TrackID = c(1, 2, 4),
  Species = c("seal", "seal", "seal")
)

analyze_reviewer_effort(raw_detections, validated_detections, group_vars = "Deployment")

}
