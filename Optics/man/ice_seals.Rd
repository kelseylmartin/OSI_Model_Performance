% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ice_seals.R
\name{ice_seals}
\alias{ice_seals}
\alias{ingest_ice_seals_csv}
\alias{calculate_ice_seals_totals}
\alias{select_ice_seals_candidates}
\title{Functions for Ice Seal Dataset Processing}
\usage{
ingest_ice_seals_csv(file_paths)

calculate_ice_seals_totals(
  left_file,
  center_file,
  right_file,
  valid_classes = c("animal", "animal_duplicate", "animal_new")
)

select_ice_seals_candidates(
  detections,
  score_threshold = 0.9,
  candidate_class = "animal"
)
}
\arguments{
\item{file_paths}{A character vector of full paths to the CSV files to be
ingested.}

\item{left_file}{Path to the left view ground truth CSV.}

\item{center_file}{Path to the center view ground truth CSV.}

\item{right_file}{Path to the right view ground truth CSV.}

\item{valid_classes}{A character vector of class names to be considered as
valid animal detections. Defaults to \code{c("animal", "animal_duplicate", "animal_new")}.}

\item{detections}{An \code{OpticsDetections} object containing data from the
Ice Seals dataset.}

\item{score_threshold}{The minimum score for a detection to be considered a
candidate. Defaults to 0.9.}

\item{candidate_class}{The category name for candidate detections.
Defaults to "animal".}
}
\value{
A single \code{OpticsDetections} object containing the combined and
standardized data from all provided files.

A named list with the total counts for "left", "center", and "right" views.

A \code{data.frame} (or \code{tibble}) containing only the candidate
detections.
}
\description{
A collection of functions specific to the ice seal dataset.
}
\section{Functions}{
\itemize{
\item \code{ingest_ice_seals_csv()}: Ingest and combine multiple Ice Seal CSV files.

This function serves as a wrapper around \code{\link{read_viame_csv}} to
ingest multiple VIAME-formatted CSV files from the Ice Seals dataset (left,
center, and right views) and combine them into a single
\code{OpticsDetections} object.

\item \code{calculate_ice_seals_totals()}: Calculate and validate total animal counts from Ice Seal ground truth files.

This function reads the ground truth CSV files for the left, center, and
right camera views, counts the number of valid animal detections in each,
and returns the totals. It also issues a warning if the number of detections
in the center view is greater than in the left or right views.

\item \code{select_ice_seals_candidates()}: Select candidate detections for review from the Ice Seals dataset.

This function filters detections from an \code{OpticsDetections} object to
identify "candidate" animals for a secondary review. Candidates are defined
as having a high detection score and belonging to a specific category (e.g., "animal").

}}
