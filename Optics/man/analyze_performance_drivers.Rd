% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{analyze_performance_drivers}
\alias{analyze_performance_drivers}
\title{Analyze and Model Drivers of Performance Disagreement}
\usage{
analyze_performance_drivers(aligned_df, group_vars)
}
\arguments{
\item{aligned_df}{A data frame containing aligned model and truth counts.
Must contain \code{model_count}, \code{truth_count}, \code{Species}, and the specified
\code{group_vars}.}

\item{group_vars}{A character vector of column names that define the unit of
analysis (e.g., \code{c("Deployment", "year")}).}
}
\value{
An object of class \code{glmerMod}, which is the fitted model from \code{lme4}.
The summary of this model can be inspected to see which factors are
significant predictors of model error.
}
\description{
This function fits a Generalized Linear Mixed-Effects Model (GLMM) to
analyze what factors influence the magnitude of error between model and
truth counts. It helps identify characteristics of images/videos (e.g., high
species richness, high abundance) that lead to larger prediction errors.
}
\details{
The function performs the following steps:
\enumerate{
\item Aggregates data by the specified \code{group_vars} (e.g., "Deployment").
\item Calculates predictor variables for each group:
\itemize{
\item \code{n_species_truth}: The number of unique species in the ground truth.
\item \code{total_individuals_truth}: The total number of individuals in the ground truth.
}
\item Calculates a \code{weighted_error} as the sum of absolute differences between
model and truth counts within the group.
\item Fits a \code{glmer} model (from the \code{lme4} package) to predict \code{weighted_error}
based on the calculated predictors, with \code{Species} as a random effect.
}
}
\examples{
\dontrun{
# Assuming 'aligned_master' is a data frame with model_count, truth_count,
# Deployment, and Species columns.

# Fit a model to see what drives errors at the Deployment level
performance_model <- analyze_performance_drivers(
  aligned_df = aligned_master,
  group_vars = "Deployment"
)

# View the results
summary(performance_model)
}
}
